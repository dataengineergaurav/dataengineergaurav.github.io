# Extractable components

No extraction is warranted. The global header/footer already live in `_layouts/default.html`, and the Insights markup is used on one route only. Keep the implementation in `insights.md` plus its existing Sass file.
